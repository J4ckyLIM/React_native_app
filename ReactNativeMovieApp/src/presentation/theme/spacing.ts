export const gridBlockSize = 8;

export const spacing = {
  xs: gridBlockSize / 2, // 4px
  s: gridBlockSize, // 8px
  sm: gridBlockSize * 1.5, // 12px
  m: gridBlockSize * 2, // 16px
  l: gridBlockSize * 3, // 24px
  ll: gridBlockSize * 4, // 32px;
  xl: gridBlockSize * 6, // 48px
  xxl: gridBlockSize * 9, // 80px
};

export type SpacingKey = 'xs' | 's' | 'sm' | 'm' | 'l' | 'xl';